# pi provides these at runtime and they are declared as peers only to document
# that. Letting pnpm auto-install them pulls a second, 200 MB copy of pi into
# the workspace and puts a `pi` shim on PATH that shadows the real one.
auto-install-peers=false
